// What CvGameCoreDLL.h provides that FDataStreamBase.h assumes. Force-included for the
// standalone test the same way the DLL build force-includes the umbrella header.
#pragma once
#include <string>
#include <vector>
typedef unsigned char byte;
typedef wchar_t wchar;
